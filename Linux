Linux Fundamentals

Linux Features:-
Reliable, Huge Base, Support, Monitory cost, open source, licencing freedom, customized, enhancement, secure

Wr linux is used?
Servers, Embedded Systems, Super Computers, Android, Gaming & Visual effect server, NASA

DLL = Dynamic Link Library

RPM = Redhat Package Management

Architecture of linux:-

Applications, Shell, Kernal, H/W

4 types of shells = BASH, C Shell , Korn Shell, Tsch Shell

BASH is the default Shell

Ctrl + L = Clear Desktop

Ubuntu - Most Popular Linux Distro

Secure & Stable

File, Run Level , Pipe

File collection of records and Run level sysmbolic link and pipe it is similar to socket but 
user can not work with it directly processes character h/w communications.

SUDO = To change 1 server to another

shutdown - h now = power off

shutdown - r now = reboot

ls-l = list

ls-l | grep

Shell scripting = Automation

Sudo-i - root of execution

vi Script.sh - create text and add the ext of the shell

Command Line Interface

Command Execution

cd = change directory

ls - list of directories

ls/opt

ls/usr

ls-a
ls-r
ls-f
ls-l

\ls = colour coding
ll - long losting

mv = move
cp = cut & paste
rm = remove the file

rm *.txt = remove all the the files

rm diru = rm -r dir4

cp , rm , mv
Interactive modes

sudo su = super users

cat = dangerous command doesn't work in interactive mode

cat > file1.txt

ctrl d = change directory

cat file1.txt -n = shown in numbers format

cat >file1.txt
umesh
ramesh
ganesg
ctrl d enter
to change directory

cat file1.txt - n
1.umesh
2.ramesh
3.ganesh

cat >file2.txt
ganesh
ramesh
umesh

cat file1.txt -n
1.ganesh
2.ramesh
3.umesh

ctrl d

cat file1.txt file2.txt >file3.txt
umesh
ramesh
ganesh
ganesh
ramesh
umesh

ctrl d

cat file3.txt -n
1.umesh
2.ramesh
3.ganesh
4.ganesh
5.ramesh
6.umesh

touch = zero byte files - multiple files we can create 

ls-ltrh

touch = back ups preforms
play imp roles

xfsdump/xfsrestore = latest back file checking

file creation method
cat
touch = zero byte files (back up)
vi/vim = imp editors text & graphical editors = vishual interface

gedit
nedit
pico
nano
joe

tail auth.log

head auth.log = 1st 10 times

tail -n20 auth log = log 20 times

less auth.log = pagewise = up down scroll

more auth.log = only scroll down

wc auth.log = word count

wc-l auth.log = lines

wc-w auth.log = words

wc-c auth.log = charater

df = disk free

-ht = type of file system

-h = file system

du -shcups = all directories

command mode = insert mode = execution mode

shift : wq = write & quit

Shift : q = quit from editor

Shift :n next = 

Shift: set nu = line number

grep = general regular expression pattern

grep -i umesh/etc/passwd

insensitive mode search

alias c = clear history




